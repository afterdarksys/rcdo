# RCDO: Ryan Coleman's Accessible DevOps Toolkit

This project is an early-stage toolkit for reviewing infrastructure changes before they cause outages.

It is being designed by a visually impaired operator who is 90% blind and works with GitHub Actions, Spacelift, OpenTofu, Ansible, AWS, Alicloud, and a custom deployment kit. The goal is to make risky changes easier to understand with a screen reader, easier to verify, and harder to approve by accident.

The intended community includes blind and low-vision DevOps engineers using screen readers or large print in boutique finance companies, smaller banks, technology companies, consultancies, and small businesses. These teams may have no prior experience supporting a blind infrastructure operator and may not own expensive enterprise tooling. The toolkit is therefore offline-first, low-dependency, auditable, and usable with ordinary pipes and files.

> Status: work-ready beta core. Every documented command builds and runs, with live read-only collectors for installed CLIs. Each employer must validate its credentials, schemas, policies, and deployment-kit adapter before production use. The checks are conservative guardrails, not proof that a deployment is safe.

## Accessible operations project

The [project plan](docs/accessible-operations/PROJECT_PLAN.md) covers all eight
workstreams for accessible senior DevOps operations. The first implementation
adds named AWS/AliCloud context checks, bounded Docker event review, persistent
reading navigation, notes, bookmarks, and evidence-linked handoffs.

```sh
rcdo context --input context.json --expect work-aws.yaml --width 72
rcdo watch --input events.json --environment staging --width 72
rcdo review-session resume --session incident.json --width 72
rcdo review-session bookmark --session incident.json --name networking
rcdo review-session note --session incident.json --note "Check target registration."
rcdo handoff --session incident.json --width 72
```

`context` compares a Missing Utils contextsnap artifact with named JSON/YAML
expectations; it does not switch the shell's active profile. `watch` reviews a
finite eventwhy artifact; continuous live monitoring remains future work.
Navigation never acknowledges findings. Stale historical reading stays available
with exit 30, and handoffs preserve unresolved blockers and label operator notes.

Run the [credential-free demo](docs/accessible-operations/DEMO.md) from the sibling
Missing Utils checkout. See [status and validation gaps](docs/accessible-operations/STATUS.md)
before using live workplace adapters. These features do not require AI credentials.

## Current foundation

The Go package in `finding/` gives every future checker one output contract:

- Validated findings with stable IDs, severity, resource, action, environment, evidence, confidence, and remediation.
- Overall `CLEAN`, `REVIEW`, `BLOCKED`, or `INCOMPLETE` status.
- Severity-first plain-text output with explicit labels and no dependence on color or visual tables.
- Versioned JSON output for CI annotations, audit records, and integrations.
- Fail-closed rendering: invalid findings are returned as errors instead of being presented as a completed review.

Run the test suite with:

```sh
go test ./...
```

Build the toolkit with:

```sh
make build
```

This creates the primary `dist/rcdo` executable and named command aliases. The
preferred style keeps every workflow under one memorable command:

```sh
./dist/rcdo tofu-check --input plan.json
./dist/tofu-check --input plan.json
```

`git-tools` and `git-diff-walker` remain compatibility aliases for existing
scripts. New scripts should use `rcdo COMMAND [options]`.

Install for one user without administrator access:

```sh
make install PREFIX="$HOME/.local"
```

Create optional user defaults and AI-provider routing:

```sh
rcdo config init
rcdo config path
```

Configuration supports global and per-command defaults with command-line
options taking precedence. API keys for OpenAI, Anthropic, and OpenRouter can be
resolved from environment variables or a separate mode-`0600` credential store,
with configurable primary, backup, and tertiary order. See
`docs/configuration.md` for the schema and security model.

## Goals

- Summarize the operational effect of a change before showing the raw diff.
- Turn nested JSON, YAML, TOML, and HCL into a linear path-based explanation.
- Compare structured configuration by path instead of punctuation and line movement.
- Identify destructive, high-blast-radius, or difficult-to-reverse changes.
- Correlate source changes with GitHub Actions jobs, Spacelift runs, and deployment targets.
- Check OpenTofu/Terraform, Ansible, workflow YAML, and shell commands for common hazards.
- Produce concise text, structured JSON, and CI annotations from the same findings.
- Make every result usable without color, mouse input, charts, or visual layout.
- Keep a human in control: explain evidence and uncertainty instead of silently changing infrastructure.

## Accessibility principles

- Plain text is the primary interface; color is optional and never the only signal.
- Findings begin with severity, resource, action, environment, and reason.
- Stable finding IDs make it possible to resume a review and skip already-reviewed items.
- Summaries are short and hierarchical so screen-reader users can navigate from risk to evidence.
- Output supports `--no-color`, `--format text`, `--format json`, and adjustable verbosity.
- Tables have a linear text alternative, and interactive prompts have non-interactive equivalents.
- Exit codes distinguish clean, warning, blocked, invalid-input, and tool-failure states.

## Toolkit commands

### `markdown-view`

Read Markdown in plain terminal text with section navigation, labelled table rows,
code blocks, search and persistent bookmarks:

```sh
rcdo markdown-view --input README --toc
rcdo markdown-view --input runbook.md --find rollback
rcdo markdown-view start --input runbook.md --state reading.json
rcdo markdown-view next --state reading.json
rcdo to-markdown --input inventory.csv | rcdo markdown-view
```

See [viewer options and saved reading](docs/markdown-viewer.md).

### `to-markdown`

Convert DOCX, PDF, CSV and XLSX to Markdown locally:

```sh
rcdo to-markdown --input report.docx --output report.md
rcdo to-markdown --input report.pdf --output report.md
rcdo to-markdown --input inventory.csv
rcdo to-markdown --input budget.xlsx --sheet Budget --table-mode records
```

DOCX uses Pandoc; PDF uses Poppler's `pdftotext`. CSV/XLSX have built-in readers.
`--output` refuses existing files. Spreadsheet display formatting and PDF OCR are
not included; missing formula results and extraction gaps remain explicit.
See [conversion options and limitations](docs/document-conversion.md).


### `config`

Manage user defaults and AI credential routing:

```sh
rcdo config set --key defaults.environment --value production
rcdo config set --key commands.config-explain.width --value 60
rcdo config ai-order --primary openai --backup anthropic --tertiary openrouter
rcdo config credential-set --provider openai --from-env OPENAI_API_KEY
rcdo config ai-status
rcdo config ai-resolve
```

Keys supplied through `--stdin` or `--from-env` never appear in toolkit output.
The credential file is permission-checked and is not part of the ordinary YAML
configuration.

### `ai-assist`

Use a configured model to explain, debug, or propose fixes for infrastructure
configuration. Every model request requires the non-configurable `--ai` flag.

```sh
rcdo config set --key providers.openai.model --value YOUR_APPROVED_MODEL
rcdo ai-assist --ai --action explain --target config --domain aws --input service.yaml
rcdo ai-assist --ai --action debug --target ansible --input playbook.yml
rcdo ai-assist --ai --action fix --target hcl --domain terraform --input main.tf
rcdo ai-assist --ai --action debug --target repo --domain docker --repo .
```

Targets are `config`, `ansible`, `hcl`, and `repo`. Domain guidance covers AWS,
AliCloud, Terraform, OpenTofu, Docker, GitHub Actions, and Ansible. Inputs are
bounded and common secrets are redacted before transmission. Repository mode
collects supported configuration files while skipping dependency, VCS, build,
and common secret paths. AI fixes are suggestions only and never write files.
See `docs/configuration.md` for the provider and data-handling details.

### `decompose` / `hcl2aws` / `hcl2ali` / `ansible2aws` / `ansible2ali`

Turn declarative infrastructure into a dependency-ordered, read-only manual
runbook. Each step contains a CLI command example, request JSON, the response
field to capture, a verification command, and a rollback command.

```sh
rcdo hcl2aws --input main.tf
rcdo hcl2ali --input alicloud.tf --region cn-hangzhou --profile work
rcdo ansible2aws --input playbook.yml
rcdo ansible2ali --input playbook.yml --region cn-hangzhou

# Generic form and one-step inspection.
rcdo decompose --from hcl --to aws --input main.tf
rcdo hcl2aws --input main.tf --step 2
```

The beta maps common VPC, subnet/VSwitch, security-group, compute-instance,
AWS object-storage bucket, and IAM/RAM role resources. Literal values are carried
into the examples and direct resource ID references become captured shell
variables. Modules, data sources, dynamic expressions, nested blocks, deletion
tasks, and unsupported resources are reported as `INCOMPLETE` for manual
resolution. No generated cloud command is executed. See
`docs/iac-decomposition.md` for the support matrix and beta workflow.
For a local, non-mutating walkthrough suitable for a first demonstration, see
`docs/beta-demo.md`.

### `review` / `git-review`

Run one accessible review over a Git revision range or the current working tree.
The command inventories changed and untracked files, performs semantic config
comparison, scans workflows and Ansible, applies dangerous-command and cloud
policy rules, and enforces the repository policy when present.

```sh
rcdo review --base origin/main --head HEAD --environment production
rcdo git-review --base HEAD --repo-policy examples/repository-policy.yaml
```

### Repository and operational policy

`.rcdo/policy.yaml` can assign owners and severities to critical paths,
require companion changes such as runbooks, and reject repository-specific
content patterns. See `examples/repository-policy.yaml`.

```sh
git diff --name-status origin/main...HEAD |
  rcdo repo-policy-check --repo-policy .rcdo/policy.yaml

rcdo ops-policy-check --input infrastructure/network.tf --environment production
```

The built-in operational pack detects public CIDRs, wildcard permissions,
disabled encryption or deletion protection, and privileged workloads.

### Safe structured editing

`config-set` and `config-remove` modify JSON, YAML, TOML, and HCL by complete path.
They show a redacted semantic preview by default and write atomically only with
`--write --expect-sha256 REVIEWED_SHA256`, using the hash from the preview.
Optional `--expect-value` asserts the existing scalar value. HCL changes use its syntax-aware writer and address attributes inside
existing block paths; they do not invent ambiguous blocks or edit inside an
attribute expression.

```sh
rcdo config-set --input service.yaml --path service.replicas --value 4
rcdo config-set --input service.yaml --path service.image --value api:v2 --string --write --expect-sha256 REVIEWED_SHA256
rcdo config-remove --input service.json --path obsolete_setting
```

### Resumable review sessions

Persist a complete versioned finding report and work through it in severity order.
Acknowledgement records reading, with a note and UTC timestamp; blocking findings
and incomplete checks remain in force. Report and optional artifact fingerprints
are rechecked on resume. Sessions expire after 24 hours by default; use --max-age
to choose a positive lifetime. Bind a clean repository with --repo to recheck HEAD
and working-tree cleanliness, and store the session outside that repository.
Legacy schema-1 sessions must be recreated from fresh versioned reports.

Start and acknowledgement return 0 on successful storage; status and next return
the report status (0/10/20/30). Stale sessions return 30 and refuse acknowledgement.
Start refuses to overwrite existing sessions.

```sh
rcdo review-session start --report review.json --change-id PR-42 --commit COMMIT
rcdo review-session next
rcdo review-session acknowledge --id FINDING-ID --note "Reviewed with platform owner"
rcdo review-session status
```

### `jsonprobe-check`

Import aggregate JSON from Missing Utils into the RCDO finding contract. Declare
required check names explicitly; missing checks, stale evidence, and inconsistent
results remain INCOMPLETE. Failed readiness checks block the review. The default
maximum evidence age is 15 minutes. Environment is a supplied review label, not
verified cloud identity. Raw upstream error messages stay in the source artifact.

```sh
rcdo jsonprobe-check --input readiness-source.json --require api-health \
  --environment staging --format json
```

See [the combined workflow](docs/missing-utils-workflow.md) for a local demo,
coverage manifests, session binding, exit codes, and evidence limitations.

### `error-explain`

Turn dense command failures into a labeled probable cause, next action, and a
small amount of redacted original evidence.

```sh
tofu apply 2>&1 | error-explain --tool tofu
kubectl apply -f service.yaml 2>&1 | error-explain --tool kubectl --format json
```

### `config-diff` (MVP available)

Compare two JSON, YAML, TOML, or HCL documents by complete configuration path.
Formatting-only changes and object-key reordering disappear, leaving added,
removed, and changed values in linear reading order. Secret changes remain
visible as changes while their values stay redacted.

```sh
# Compare two revisions of the same configuration.
rcdo config-diff --before production-old.yml --after production-new.yml

# Limit the review to one operational area.
rcdo config-diff --before old.tf --after new.tf --path resource.aws_db_instance

# Return exit code 10 when a semantic change exists.
rcdo config-diff --before old.json --after new.json --check

# Compare documents written in different formats.
rcdo config-diff \
  --before defaults.json \
  --after defaults.yaml \
  --format json
```

Arrays are compared by index. HCL expressions are compared as normalized source
expressions; the command does not evaluate variables, providers, or functions.

### `config-explain` (MVP available)

Read structured configuration without relying on indentation, brace matching, or
visual alignment. The command accepts JSON, YAML, TOML, and HCL, then emits one
labeled path at a time. Input syntax is detected from the filename or content,
and likely secret values are redacted unless `--show-secrets` is explicit.

```sh
# Explain a file from the root, including scalar values.
rcdo config-explain --input environments/production.tfvars

# Focus speech or magnification on one subtree.
rcdo config-explain --input inventory.yml --path all.children.production

# Hear structure without reading every value.
rcdo config-explain --input config.toml --values=false

# Produce deterministic machine-readable output.
kubectl get deployment api -o json | config-explain --format json
```

Each entry has a complete path, kind, optional source line, and optional value.
Use `--syntax` when extensionless or ambiguous input cannot be identified safely.

### `pr-manager` (MVP available)

Create, inspect, and update pull requests. `inspect` reviews GitHub JSON for stale commits, conflicts, approvals, and failed checks. `create` and `update` produce a readable preview and invoke GitHub CLI only when `--execute` is explicitly supplied.

### `rcdo diff-walk` (MVP available)

Walk a diff one logical change at a time. Provide screen-reader-friendly summaries, optional color, search, file and hunk navigation, and a way to mark findings as reviewed.

The current MVP reads a unified Git diff without invoking Git or changing the repository. It supports linear text, structured JSON, summary-only output, case-insensitive search of changed lines, and one-based file and hunk selection.

```sh
# Review a working-tree diff.
git diff | ./dist/rcdo diff-walk

# Hear the summary before choosing a file or hunk.
git diff | ./dist/rcdo diff-walk --summary-only
git diff | ./dist/rcdo diff-walk --file 2 --hunk 1

# Find changed lines concerning production.
git diff | ./dist/rcdo diff-walk --search production

# Save structured output for another tool.
git diff | ./dist/rcdo diff-walk --format json > diff-review.json

# Review an existing patch without touching a repository.
./dist/rcdo diff-walk --input proposed-change.diff
```

Run `./dist/rcdo diff-walk --help` for every option. Optional color and persistent reviewed-item tracking remain planned features; neither is required for nonvisual use.

### `git-danger-check` (MVP available)

Detect dangerous operations involving tools such as `aws`, `aliyun`, `gcloud`, `oci`, `docker`, and `kubectl`. Flag destructive commands, wildcard targets, production access, privilege changes, unpinned artifacts, unsafe shell expansion, and missing confirmation or rollback controls.

### `git-isimportant-check` (MVP available)

Use a configuration file such as `giimp_check.conf` to identify important files, resources, and lines. Warn about deletion of critical configuration, unusually large changes, broken protections, and changes that require specific reviewers.

### `git-update-json` (MVP available)

Safely deep-merge OpenTofu/Terraform JSON at an explicit path using a supplied JSON object. Validate and print the complete proposed document, preserve unrelated object keys, and require `--write` before atomically replacing a file.

Example use case: add or update an AWS VM configuration from `template.json`.

### `gha-tool` (MVP available)

Review and maintain GitHub Actions workflows:

- `check`: scan permissions, expressions, action references, runner trust, and secret-handling hazards.
- `update --template <file.yml>`: preview an approved workflow template and replace the named workflow only with `--write`.
- `fix`: normalize safe textual formatting and report dangerous patterns that still require a human decision.
- `fmt`: normalize line endings and trailing whitespace without changing workflow structure.
- `dangerous`: find overbroad permissions, unpinned actions, secret exposure, unsafe pull-request triggers, script injection, and risky deployment steps.

Update and formatting operations preview to standard output. A file changes only when `--write` is supplied.

### `tofu-check` (MVP available)

Review `tofu show -json` plan output. Detect deletes, replacements, stateful-resource impact, IAM/policy updates, and common public-access indicators. Deeper provider, backend, encryption, and organization-specific policies remain policy-pack work.

### `spacelift-check` (MVP available)

Review explicit run identity, state, policy/approval evidence, freshness,
supersession, dependency and drift observations. Bind exact plan JSON bytes to
supplied run/commit acquisition evidence and compare stack settings with explicit
expectations. Basic live collection queries the actual run through `spacectl api`;
missing policy and operational evidence stays INCOMPLETE.

### Expanded Spacelift and Terraform/OpenTofu workflows

```sh
rcdo tofu-check --engine terraform --plan review.tfplan
rcdo plan-explain --input plan.json --resource module.database
rcdo plan-diff --before reviewed-plan.json --after new-plan.json
rcdo iac-config-check --input main.tf
rcdo iac-validate --engine tofu --native --directory infrastructure
rcdo iac-context --input context.json --expect expected-context.json
rcdo spacelift-runs --input runs.json --state FAILED
rcdo spacelift-diff --before old-run.json --after new-run.json
rcdo spacelift-watch --stack production --run RUN_ID --samples 6 --interval 5s
rcdo command-gen --to aws --input main.tf --region us-east-1
rcdo command-gen --to spacelift --input stack-command.yaml --action logs
rcdo command-gen --to terraform --input main.tf --action plan
```

Plan explanations cover replacement paths/order, unknown/sensitive values,
resource moves/imports, output changes and security changes. Finding JSON feeds
existing resumable navigation and bookmarks. Command generation prints recipes
without executing them; unsupported translations remain explicit.

See [schemas, workflows and coverage limitations](docs/spacelift-iac.md).

### `ansible-check` (MVP available)

Perform parsed-YAML checks for broad host patterns, shell usage, ignored failures, possible plaintext credentials, uncontrolled package upgrades, and always-changed tasks. Quotes, comments, sequence markers and flow mappings preserve the same checks. Unresolved includes and roles remain incomplete. Use it alongside `ansible-lint`, syntax checks, check mode, and integration tests.

### Terraform-to-Ansible workflow review

`workflow-check` traces Terraform/OpenTofu outputs to effective Ansible host
variables, identifies extra-vars overrides, and checks source hashes, types,
identity, freshness and selected hosts. Execution and verified stages require
additional bound invocation, callback and outcome evidence.

```sh
rcdo workflow-check --manifest workflow.json --width 60
rcdo workflow-check --manifest workflow.json --stage verified
python3 scripts/workflow-practice.py
```

See [workflow contracts and local native practice](docs/terraform-ansible-workflow.md).

`workflow-trace` follows variables into tasks, local roles and templates.
`workflow-collect` acquires bound evidence from GitHub Actions or explicit
Spacelift/custom exports and runs the same staged review. The workplace recorder
captures actual producer, Ansible and independent health evidence. `pilot --suite
workflow` retains the operator's accessibility acceptance observations.

See [workplace setup, adapters and acceptance](docs/workplace-workflow.md).
Run `python3 scripts/workflow-integration-practice.py` for portable collection
practice, or add `--native --engine terraform` (or `tofu`) for a localhost run.
The guide also covers stricter `deploy-review` validation and the provenance and
source bindings now required by `review-change`.

### `deploy-review` (MVP available)

Combine finding reports from the pull request, GitHub Actions, OpenTofu, Spacelift, Ansible, cloud context, and custom deployment-kit adapters. Missing or invalid component reports make the combined review explicitly `INCOMPLETE`.

Name reports and declare the required coverage explicitly:

```sh
rcdo deploy-review \
  --report pull-request=reports/pr.json \
  --report opentofu=reports/tofu.json \
  --require pull-request \
  --require opentofu \
  --require cloud-context
```

### `review-change` (MVP available)

Apply one versioned coverage manifest to an entire infrastructure change. The manifest binds a change ID, commit, environment, required toolchain components, and their finding reports. Relative report paths resolve from the manifest directory. A missing, unreadable, or invalid required report makes the review `INCOMPLETE`.

```sh
rcdo review-change --manifest review-change.json
```

### `cloud-context-check` (MVP available)

Compare an AWS or Alicloud identity snapshot with an explicitly expected cloud, account, and region. A mismatch is critical. A missing field makes the review incomplete. This is intended to stop correct changes from reaching the wrong account.

### `runbook-check` (MVP available)

Check deployment instructions for rollback, validation, ownership, target scope, and stop conditions. Output uses explicit labels instead of assuming a reader can interpret a visual flowchart.

### `review-brief` (MVP available)

Turn a finding JSON report into a low-density brief with configurable line width. It presents only result, severity, title, target, and next action, making it suitable for large print, high magnification, speech, or a stressful incident bridge.

### `a11y-output-check` (MVP available)

Test CLI output for ANSI control sequences, tab-based alignment, and lines that exceed a configurable magnified-reading width. This lets contributors catch accessibility regressions in CI.

### `evidence-pack` (MVP available)

Create a deterministic JSON manifest containing the reviewed commit, change ID, artifact names, sizes, and SHA-256 hashes. It supports audit and handoff workflows without copying the contents of proprietary plans into the manifest.

## Exit codes

Review commands use stable exit codes suitable for CI and speech-friendly shell wrappers:

- `0`: clean or successful operation
- `10`: findings require review
- `20`: high or critical findings block the change
- `30`: review is incomplete
- `2`: invalid arguments or input

The full normalized input contracts and pipeline examples are in `docs/input-formats.md`.

Every review command also supports:

- `--format github` for escaped GitHub Actions annotations.
- `--format sarif` for SARIF 2.1.0 output.
- `--policy FILE` for owner-attributed, expiring suppressions.
- Completed-check and incomplete-check coverage in text and JSON reports.

Live collectors are opt-in. See `docs/workflows.md` for GitHub CLI, OpenTofu, Spacelift, AWS, Alicloud, native syntax-checker, policy, and CI examples.

## Suggested review flow

1. Confirm the commit, branch, target account, region, and environment.
2. Summarize changed files and rank them by operational risk.
3. Validate GitHub Actions, OpenTofu/Terraform, Ansible, and deployment-kit inputs.
4. Compare configuration changes with the actual Spacelift plan.
5. Present destructive actions and security-boundary changes first.
6. Require an explicit acknowledgement for blocking findings or documented exceptions.
7. Save a machine-readable review record for CI, audit, and later comparison.

## High-value safety checks

- Resource deletion or replacement, especially stateful services.
- IAM privilege expansion, trust-policy changes, and new public access.
- Security-group, firewall, route, DNS, load-balancer, and certificate changes.
- State backend, provider, account, subscription, region, or workspace changes.
- Secrets printed to logs or passed through unsafe shell interpolation.
- GitHub Actions with broad `GITHUB_TOKEN` permissions or mutable third-party action tags.
- Ansible plays targeting more hosts than expected or using `shell` where a module is safer.
- Plans generated from a different commit, variable set, stack, or environment.
- Missing health checks, rollout limits, validation steps, backups, or rollback instructions.
- AWS/Alicloud naming or identity mismatches that could send a change to the wrong cloud account.

## Design guardrails

- Start read-only and advisory. Any write or deployment action must be explicit and previewable.
- Parse structured output where available instead of scraping colored terminal text.
- Never treat a successful syntax check as proof that a change is safe.
- Show the rule, evidence, affected target, confidence, and suggested next check for each finding.
- Support repository-specific policies and documented suppressions with an owner, reason, and expiry.
- Redact secrets from output and test fixtures.
- Degrade clearly when an API, credential, or upstream service is unavailable; do not report a partial review as complete.

## Initial roadmap

1. **Complete:** Shared finding format and accessible text/JSON renderers.
2. **Complete:** Working MVPs for every command in the toolkit.
3. **Complete:** OpenTofu plan JSON, GitHub pull-request snapshot, Spacelift snapshot, and cloud-context inspection.
4. **Complete:** Path-based explanation and semantic comparison of JSON, YAML, TOML, and HCL with default secret redaction.
5. **Complete:** Unified Git review, repository policy, operational policy pack, resumable sessions, safe JSON/YAML/TOML/HCL mutation, and error narration.
6. **Next:** Organization-specific AWS and Alicloud policy extensions and deployment-kit adapters.
7. **In progress:** Live read-only collection and required-component coverage reporting. Coverage manifests are available; richer collector provenance remains next.
8. **In progress:** CI annotations and expiring suppressions are available. Signed evidence and incident regression fixtures remain next.

## Contributing ideas

Useful contributions include anonymized incident patterns, dangerous-change rules, accessible CLI testing, fixture repositories, cloud-specific policy checks, and adapters that do not expose credentials or proprietary deployment details.

Expanded roadmap: [ROADMAP.md](ROADMAP.md); guarded writes require the SHA-256 from the preview.

Execution receipts and configuration navigation
-----------------------------------------------

`receipt-review --input RECEIPT.json` reads Missing Utils runreceipt evidence and
reports each pipeline stage. Exit zero describes local process completion only;
remote outcomes require independent verification. Incomplete receipts never
justify automatic retries.

`config-walk start --input FILE --state navigation.json` starts persistent
structural navigation. Use `show`, `parent`, `child`, `next`, `previous`, `find`,
`goto`, and `bookmark` with the same state file. Text output is linear and wrapped;
`--format json` retains exact paths. Changed source versions are announced and
missing paths require explicit relocation. See docs/accessible-operations/DEMO.md
for the offline cross-tool demonstration and current limitations.

## Accessible investigation workflows

The [roadmap feature guide](docs/roadmap-features.md) documents `log-read`,
`context-summary`, `resource-walk`, `incident`, `runbook`, `fleet-check`,
`report-read` and `doctor`, including input schemas and evidence limitations.
Each has a separate feature commit. Start with credential-free practice:

```sh
make build
python3 scripts/accessible-practice.py
rcdo doctor --sample
rcdo report-read --input report.json --layout braille
```

The practice runner retains synthetic fixtures and transcripts. Live collection
coverage and actual assistive-technology acceptance remain explicit follow-ups in
[ROADMAP.md](ROADMAP.md) and the [pilot worksheet](docs/accessible-operations/PILOT.md).

## Live operations priorities

[Live operations guide](docs/live-operations.md) covers the new `collect`, `changes`,
`kube-explain`, and enhanced `command-gen --explain --shell posix|powershell` workflows.
AWS collection requires an explicit expected account and region. Kubernetes native
collection requires an explicit context and namespace. Both retain incomplete
coverage instead of treating failed collection as healthy infrastructure.

Run `python3 scripts/live-operations-practice.py` after building for synthetic
adapter-to-reader checks without cloud requests or employer credentials.

## Operator workflows

The [operator workflow guide](docs/operator-workflows.md) covers `ansible-watch`,
`state-walk`, `network-check` and `tasks`. It includes the opt-in Ansible callback,
sensitive state handling, explicit endpoint probes and resumable task registration.
Run `python3 scripts/operator-workflows-practice.py` after building for synthetic
practice with a localhost-only network endpoint.

## Audit, monitoring and acceptance

The [continuation guide](docs/next-six.md) covers audit investigation and retention,
live local-file announcements, permission-scope comparisons, native context
acquisition, HCL references, identity bookmarks and operator acceptance sessions.
Run `python3 scripts/next-six-practice.py` after building for temporary local
scenarios, including installed Ansible inventory and OpenTofu local-backend checks.
Actual assistive-technology and authenticated cloud acceptance remain separate.

Local Rego policies: `rcdo rego-check --rego rules.rego --input config.json`.
Requires OPA 1.x on PATH. See docs/rego.md and examples/rego/terraform.rego for
structured decisions, CI exit codes, restricted builtins, and plan checks.
Policy workflow batch: rego-test runs fixture suites, rego-diff compares policy
versions, and policy-review combines IaC and Rego results. Five starter safeguards
and complete usage are in docs/policy-workflows.md.
